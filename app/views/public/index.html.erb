<% @tytul_strony = "Strona Główna" %>

<!-- Góra strony poczatek  -->
<div class="header-image">
  <div class="headline">
    <div class="container">
      <h2>Hurtownia BHP</h2>
    </div>
  </div>
</div>
<!-- Góra strony koniec -->

<div class="container marketing">

  <!-- Trzy kolumny zdjęcia + opis + linki początek -->
  <div class="row">
    <% @product.each do |product| %>
      <div class="col-lg-4">
        <%= image_tag(product.picture.url(:thumb), :class => "img-circle") %>
        <h2>
          <% product.name %> </h2>
        <p><%= product.name %></p>
        <p><%= link_to("Zobacz produkt >>", {:action => 'product', :id => product.id}, :class => 'btn btn btn-info') %> </p>
      </div>
    <% end %>
  </div>

  <!-- Galerie koniec -->

  <!-- Trzy kolumny zdjęcia + opis + linki koniec -->

  <!-- Galerie poczatek -->

  <!-- Galerie koniec -->
</div>
